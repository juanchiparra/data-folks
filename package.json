{
	"name": "docs",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "pnpm run sync-readme && vite dev",
		"build": "pnpm run sync-readme && vite build",
		"preview": "vite preview",
		"deploy": "gh-pages -d build -t",
		"sync-readme": "node scripts/sync-readme.js"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^3.0.0",
		"@sveltejs/adapter-static": "^3.0.8",
		"@sveltejs/kit": "^2.15.1",
		"@sveltejs/vite-plugin-svelte": "^6.2.4",
		"svelte": "^5.0.0",
		"vite": "^7.3.1"
	},
	"overrides": {
		"cookie": "^0.7.0"
	},
	"dependencies": {
		"gh-pages": "^6.3.0"
	}
}